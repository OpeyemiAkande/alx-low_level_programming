#include <stdio.h>
#include "main.h"
/**
 * _putchar - The function to use in the place of putchar
 * @c: the character to be printed to screen
 * Return: 0 (success)
 */
int _putchar(int c)
{
	putchar(c);
	return (0);
}
